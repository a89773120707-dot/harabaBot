# Daily Report: 2026-06-11 (UPDATE — вечер)

## ✅ Что сделано после первого отчёта

### Feedback V2 — новая модель реакций
- 🟢🟡🔴 → 👀 Посмотреть / 🤔 Подумать / ⏭ Скип
- Каждая реакция имеет причины (inline кнопки)
- Реакция + причина → сразу сохраняется в БД
- Комментарий только при нажатии "💬 Написать комментарий"

### UX итерации (3 версии за день)

**UX v1:**
- 2 клика для стандартных причин (без комментария)
- Комментарий только для *_other и need_more_info

**UX v2:**
- Разделение на Основные (5) + Ещё причины
- Кнопка ⬅️ Назад
- Кнопка ➕ Ещё причины
- Промпты: "Что понравилось?" / "Что мешает?" / "Почему пропускаем?"

**UX v3 (финальная):**
- 4 основные причины + 💬 Написать комментарий
- Убраны дубли "Без причины"
- Убраны "Ещё причины"
- Убраны второстепенные причины из основного экрана

### Причины по реакциям (UX v3)

**👀 Посмотреть (review):**
- 💰 Хорошая цена
- 📉 Небольшой пробег
- 🔥 Ликвидная модель
- ⚙ Хорошая комплектация
- 💬 Написать комментарий

**🤔 Подумать (think):**
- 💸 Высокая цена
- 📈 Большой пробег
- 👥 Много владельцев
- ⚙ Слабая комплектация
- 💬 Написать комментарий

**⏭ Скип (skip):**
- ❌ Слишком дорого
- ❌ Слишком большой пробег
- ❌ Плохое состояние
- ❌ Юр. риски
- 💬 Написать комментарий

### Веса причин
- Добавлены REASON_WEIGHTS в `ris_reason_keyboard.py`
- Хранятся в коде, НЕ используются для скоринга
- Пример: high_price=10, good_price=10, bad_color=2

### Баг-фиксы реакций
- Исправлен `get_last_feedback_id()` (row[0] вместо row["max_id"])
- Исправлен `_save_feedback_for_chat` (передаём `update.effective_user` вместо `context`)
- Исправлен `reply_to_message_id` в `edit_message_text` → `reply_text`
- Синхронизированы локальные статусы с VPS (protocol_skrin active, ismailovvv97 paused)
- Очищен dedup на VPS (279 записей удалено)

### Диагностика рассылки
- Выявлена проблема: локальная БД ≠ VPS БД по статусам менеджеров
- Протокол: проверять статусы на VPS через `crontab`, `get_enabled_recipients()`, `sent_ads`
- Cron: `0 */1 * * *` — каждый час

---

## 📊 Статистика на конец дня

| Показатель | Значение |
|---|---|
| Реакций в БД (VPS) | ~40+ |
| Реакций в БД (локально) | ~40+ |
| Получателей active (VPS) | 2 (protocol_skrin + owner) |
| Получателей paused (VPS) | 1 (ismailovvv97) |
| Карточек отправлено сегодня | ~20 |
| Коммитов сегодня | 7+ |
| Файлов admin_bot/ | 17 |

---

## 📋 Что осталось

### ⏳ Ближайшие (приоритет)
1. **Fix 7: Привязать реакцию к config_name** — каждая реакция должна сохранять `config_name` из sent_ads для будущего config_report
2. **Накопить 50-100 реакций** — отправлять карточки, собирать feedback
3. **Проверить что reason_code сохраняется** — после каждого выбора причины

### ⏳ Среднесрочные (после 100+ реакций)
4. **config_suggestions** — анализировать реакции по конфигам и предлагать изменения
5. **/config_report** — отчёт по каждому конфигy: реакции, причины, тренды
6. **/approve_config_change** — owner подтверждает изменения конфигов

### ⏳ Долгосрочные
7. **learning_dashboard** — визуализация трендов реакций
8. **semi-auto режим** — автоматическое применение правил при confidence > 90%

---

## 🚫 Заморожено (не делать)
- learning_score — бонус к скорингу
- reaction_learning_scorer.py — интеграция с scoring
- Автоматическое изменение score
- Автоматическое изменение конфигов

---

## 📦 Коммиты сегодня

```
2ff2dec Feedback V2 UX v3 — clean reasons, no duplicates, comment button
8e60016 Add RIS analytics — learning_report, learning_reasons, config_report
5423105 Add admin bot 🧠 Обучение section
b9bdd56 Add RIS Block 1 — Reaction Intelligence System
ba88ea3 Add admin bot and recipient management
c25c016 Connect telegram_sender to telegram_users status
c7f29f3 Add Haraba Mini admin bot MVP
```

---

## 🏗 Архитектура проекта (обновлённая)

```
haraba-mini/
├── admin_bot/                          # Админ-бот (systemd service)
│   ├── admin_bot.py                    # точка входа
│   ├── config.py                       # .env загрузка
│   ├── permissions.py                  # is_owner, is_admin
│   ├── keyboards.py                    # inline кнопки (🧠 Обучение добавлена)
│   ├── formatting.py                   # форматирование сообщений
│   ├── handlers/
│   │   ├── start.py                    # /start, /menu
│   │   ├── menu.py                     # главное меню + navigation
│   │   └── learning.py                 # 🧠 Обучение (3 отчёта)
│   └── services/
│       ├── db_service.py               # миграции, БД
│       ├── users_service.py            # менеджеры
│       ├── reactions_service.py        # реакции
│       ├── stats_service.py            # статистика
│       ├── cards_service.py            # карточки
│       ├── searches_service.py         # поиски
│       └── logs_service.py             # логи
│
├── telegram_feedback_bot.py            # Feedback bot (systemd service) — V2 UX
├── telegram_sender.py                  # Рассылка карточек
├── feedback_store.py                   # SQLite feedback + recipients + JOIN
├── ris_analytics.py                    # learning_report, learning_reasons, config_report
├── ris_reason_keyboard.py              # Inline кнопки причин (27 штук, веса, промпты)
├── ris_reason_store.py                 # save_reaction_detail, needs_comment
├── ris_migration.py                    # Миграция RIS таблиц
├── run_daily_pipeline.py               # Daily pipeline (cron)
├── card_data_loader.py                 # Загрузка карточек
├── config_loader.py                    # Загрузка конфига
├── config/
│   └── awd_liquid_full_config.yaml     # 17 моделей
└── results/
    └── feedback.db                     # SQLite база
```

---

## 🎯 Следующий шаг

1. **Привязать реакцию к config_name** (Fix 7)
2. **Копить реакции** — отправлять карточки каждый день
3. **После 100 реакций** → config_suggestions
